server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookverse?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Thanvi@1806
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
        
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB

# JWT Configuration Parameters
app:
  jwt:
    secret: 9a4f98d8e82d8c3f2d2427a7c0628e932454a8b8c2d113674697968538741362 # 256-bit Hex Encoded Key
    expiration-ms: 86400000 # 24 hours in milliseconds
    header: Authorization
    prefix: "Bearer "
